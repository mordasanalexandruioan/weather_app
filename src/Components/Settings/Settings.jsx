import React from "react";
import { SettingsContext } from "../../Context/settingsContext";

const Settings = () => {
  return <SettingsContext.Consumer></SettingsContext.Consumer>;
};

export default Settings;
